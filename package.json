{
  "name": "bma-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/bma-web",
    "build": "npm run build --workspace=apps/bma-web",
    "start": "npm run start --workspace=apps/bma-web",
    "lint": "npm run lint --workspace=apps/bma-web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
